{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "MPI+OMP debug build",
            "type": "shell",
            "command": "mpic++ -fopenmp -DDEBUG -g -o ${workspaceFolder}/DDZ_A/bin/prefsum.out ${workspaceFolder}/DDZ_A/main.cpp",
            "group": "build"
        }
    ]
}